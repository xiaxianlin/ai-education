今日练习会话创建逻辑：

1. 检查当天是否已经生成会话，如果已经生成则执行一步
2. 检查是否存在历史未完成会话，如果存在则更新为今天练习，并重置进度，其他情况执行一下生成题目
3. 如果没有则生成固定 30 道题，题目生成逻辑如下：
    1. 根据当前学生学习的教材尝试去题目表里召回 15 道题
    2. 如果无法召回或者召回不足，余下的数量都由 AI 生成

按要求对 sever 重构：

1. 今日练习、单元练习、能力评估共用一张会话表，使用 session_type 区分
2. 今日练习、单元练习、能力评估共用一套题目生成逻辑，其中不同点如下：
    1. 数据库题目召回策略每个有一套自己的策略
    2. AI 生成的 prompt 指令使用各自的指令

题目生成逻辑：

1. 题目固定数量为 30 道
2. 根据指定 session_type 的策略去题目表里召回 15 道题
3. 如果无法召回或者召回不足，余下的数量都由 AI 生成

其他：
重构完成后删除无用代码
根据重构的服务和接口同步重构后台管理系统和学生系统