{
  "name": "ai-education-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AI Education Platform Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "dev:admin": "cd apps/admin-web && pnpm dev",
    "dev:student": "cd apps/student-web && pnpm dev",
    "dev:app": "cd apps/student-app && flutter run",
    "dev:server": "cd apps/server && uv run uvicorn main:app --reload --host 0.0.0.0 --port 7890",
    "dev:all": "concurrently \"pnpm dev:admin\" \"pnpm dev:student\" \"pnpm dev:server\"",
    "build:admin": "cd apps/admin-web && pnpm build",
    "build:student": "cd apps/student-web && pnpm build",
    "build:server": "cd apps/server && uv build",
    "build:app": "cd apps/student-app && flutter build",
    "build:app:ios": "cd apps/student-app && flutter build ios",
    "build:app:android": "cd apps/student-app && flutter build apk",
    "build:all": "turbo run build",
    "install:all": "pnpm install && cd apps/server && uv sync && cd ../student-app && flutter pub get",
    "generate:types": "tsx scripts/generate-types.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "concurrently": "^8.2.2",
    "openapi-typescript": "^6.7.0",
    "prettier": "^3.2.5",
    "tsx": "^4.7.0",
    "turbo": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
