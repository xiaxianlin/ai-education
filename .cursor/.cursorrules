# AI 教育辅导平台 - Cursor 规则

## 项目概述
这是一个 K12 教育辅导工具的 Monorepo 项目，使用 pnpm workspace 和 Turborepo 进行管理。

## 项目结构

### 前端应用
- **apps/admin-web**: 管理后台前端 (React 18 + Rsbuild + Ant Design 5)
  - 使用 TypeScript 5
  - 使用 Less + Tailwind CSS
  - 使用 Rsbuild 构建
  - 状态管理: ahooks
  - HTTP 客户端: Axios

- **apps/student-web**: 学生端前端 (React 18 + Rsbuild + shadcn/ui)
  - 使用 TypeScript 5
  - 使用 Tailwind CSS
  - 使用 Rsbuild 构建
  - 状态管理: Zustand
  - 路由: react-router-dom
  - HTTP 客户端: Axios

### 后端服务
- **apps/server**: 服务端单体 (Python 3.12 + FastAPI)
  - 使用 FastAPI 0.115+ 框架
  - 使用 SQLAlchemy 2.0 异步 ORM
  - 数据库: MySQL
  - 缓存/队列: Redis + Celery (任务队列)
  - AI 工作流: LangChain + LangGraph（题目生成等）
  - 认证: JWT (PyJWT)
  - AI 平台: 阿里云百炼AI (DashScope SDK)
  - 对象存储: 阿里云 OSS
  - 日志: Loguru
  - 使用 uv 作为包管理器

### 移动应用
- **apps/student-app**: 移动应用 (Flutter 3.0+)
  - 使用 Dart 3.8+
  - 状态管理: Riverpod (flutter_riverpod, hooks_riverpod)
  - 路由: GoRouter 17.0
  - 网络请求: Dio 5.4.0
  - 本地存储: SharedPreferences 2.2.2
  - UI 组件: Material Design + 自定义组件
  - 代码生成: json_serializable, freezed, build_runner
  - 音频录制: record 6.1.2
  - 权限管理: permission_handler 12.0.1
  - 图片缓存: cached_network_image, flutter_cache_manager
  - 支持 iOS 和 Android

## 编码规范

### TypeScript/JavaScript
- 使用 TypeScript 进行类型检查
- 遵循 ESLint 规则
- 使用 Prettier 进行代码格式化
- 组件使用函数式组件和 Hooks
- 优先使用函数式编程风格

### 页面编码规范（通用）

#### 目录结构
```
pages/[Feature]/[PageName]/
├── index.tsx                    # 页面入口
├── models/
│   └── PageModel.ts            # 页面级状态管理（使用 unstated-next）
├── views/
│   └── Main.tsx                # 主视图组件
├── hooks/
│   └── use[PageName]Hook.ts    # 业务逻辑 Hook
└── components/
    └── [ComponentName]/
        ├── index.tsx           # 组件入口
        ├── types.ts            # 类型定义
        └── [SubComponent].tsx  # 子组件
```

#### 状态管理模式
- 页面级状态使用 `unstated-next` 的 `createContainer`
- 从全局 Model 获取数据，计算派生状态
- 导出 `PageModel` 和 `usePageModel` Hook

#### 页面入口规范
- 默认导出页面组件
- 使用 `PageModel.Provider` 包裹视图
- 入口文件保持简洁

#### Hook 设计规范
- Hook 命名以 `use` 开头
- 按顺序组织：状态 → 请求 → 副作用 → 操作 → 返回值
- 使用 `useRequest` 管理异步操作
- 返回对象包含数据和操作方法

#### 组件组织规范
- 组件目录使用 `index.tsx` 作为入口
- 主组件负责状态管理和分发
- 子组件负责具体 UI 渲染
- 类型定义统一放在 `types.ts`

#### 导入顺序规范
1. React 相关
2. 第三方库
3. 业务组件
4. UI 组件
5. 类型定义
6. 工具函数/常量

#### 命名规范
- 组件：PascalCase（如 `WaitCard.tsx`）
- Hook：camelCase，以 `use` 开头（如 `useDailyPractice.ts`）
- 函数：camelCase（如 `handleClick`）
- 常量：UPPER_SNAKE_CASE（如 `PRACTICE_STATUS`）
- 类型/接口：PascalCase（如 `PracticeCardProps`）

#### 代码组织原则
1. 单一职责：每个文件/函数只做一件事
2. 关注点分离：状态、视图、逻辑分离
3. 可复用性：通用逻辑提取为 Hook
4. 可维护性：清晰的目录结构和命名
5. 一致性：遵循统一的代码风格

### Python
- 遵循 PEP 8 代码风格
- 使用类型提示 (Type Hints)
- 使用 FastAPI 的依赖注入系统
- API 路由使用 Pydantic 进行数据验证
- 使用异步编程 (async/await)

### Flutter/Dart
- 使用 Dart 3.8+ 语言特性
- Widget 使用函数式组件和 StatefulWidget/StatelessWidget
- 遵循 Flutter 最佳实践
- 使用 Material Design 进行 UI 设计
- 使用 Riverpod 进行状态管理
- 使用 json_serializable 和 freezed 进行代码生成
- 优化性能和内存使用

## 文件命名规范
- Python 文件: 使用 snake_case (例如: `user_service.py`)
- TypeScript/React 组件: 使用 PascalCase (例如: `UserProfile.tsx`)
- 工具函数: 使用 camelCase (例如: `formatDate.ts`)
- 常量文件: 使用 UPPER_SNAKE_CASE (例如: `API_CONSTANTS.ts`)

## 开发工作流
1. 前端开发: 使用 `pnpm dev:admin` 或 `pnpm dev:student`
2. 后端开发: 使用 `pnpm dev:server` 启动服务端（FastAPI 单体 + 任务）
3. 移动应用: 使用 `cd apps/student-app && flutter run`（需要先运行 `./build.sh` 生成代码）
4. 全部启动: 使用 `pnpm dev:all` 启动所有前端和后端服务

## 依赖管理
- Node.js 项目: 使用 pnpm (版本 >= 8.0.0, packageManager: pnpm@8.15.0)
- Python 项目: 使用 uv 进行依赖管理
- Flutter 项目: 使用 `flutter pub get` 进行依赖管理，使用 `build_runner` 生成代码

## 数据库
- 使用 MySQL 数据库
- 初始化脚本位于 `infra/mysql/00-init.sql`

## 注意事项
- 所有 API 端点应该包含适当的错误处理
- 使用环境变量管理配置信息
- 遵循 RESTful API 设计原则
- 前端组件应该可复用且易于测试
- 后端服务应该包含适当的日志记录（使用 Loguru）
- 使用 Docker 进行容器化部署
- 前后端类型定义保持一致（通过 packages/shared-frontend）
- 移动端应参考 Web 端（student-web）的实现逻辑，保持功能一致性
- 使用异步编程（async/await）处理 I/O 操作
- 遵循项目的分层架构：路由层 → 服务层 → 数据层

## 代码审查要点
- 检查类型安全 (TypeScript/Python 类型提示)
- 确保错误处理完善
- 验证 API 响应格式一致性
- 检查代码可读性和可维护性
- 确保遵循项目的代码风格
- 验证前后端数据模型一致性
- 检查性能优化（缓存、查询优化、代码分割）
- 确保安全性（认证、授权、数据验证）
- 验证跨平台功能一致性（Web 和移动端）

## 角色切换
项目提供了详细的角色切换指南，请参考 `.cursor/roles.md` 文件了解如何在不同开发角色之间切换：
- `@frontend` - 前端开发者
- `@backend` - 后端开发者
- `@app` - 移动端开发者
- `@architect` - 架构师
- `@fullstack` - 全栈开发者
- `@ui-designer` - UI 设计师
- `@admin-web`, `@student-web`, `@student-app`, `@server` - 应用专用命令

