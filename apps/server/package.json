{
  "name": "server-api",
  "version": "0.1.0",
  "private": true,
  "description": "AI Education Platform Server",
  "scripts": {
    "dev": "uv run main.py",
    "build": "uv build",
    "start": "uv run main.py",
    "test": "uv run pytest"
  }
}
