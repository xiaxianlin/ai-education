{
  "name": "student-app",
  "version": "1.0.0",
  "private": true,
  "description": "AI Education Platform - Flutter Mobile App",
  "scripts": {
    "install": "flutter pub get",
    "build": "flutter build",
    "build:ios": "flutter build ios",
    "build:android": "flutter build apk",
    "analyze": "flutter analyze",
    "format": "flutter format .",
    "test": "flutter test",
    "codegen": "./build.sh"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

